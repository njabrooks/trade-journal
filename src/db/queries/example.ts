// Example query functions
// Replace with actual queries as you build out the application

import { db } from '../index';
// import { yourTable } from '../schema';

/**
 * Example query function structure
 * 
 * export async function getExampleData() {
 *   return await db.select().from(yourTable);
 * }
 */

