<!-- Generated by the Workspace CLI. Edit provider-entry-points.json or its authored sources, then regenerate. -->
# Provider Entry Point: codex

## Provider-neutral repository guidance

### CONTEXT.md

# Trade Journal domain and operating context

Trade Journal is a single-context personal investment system. This file is the provider-neutral canonical
entry point for its domain vocabulary and repository-critical operating rules. `CLAUDE.md` and `AGENTS.md`
package these rules for particular agent runtimes; they do not override this shared context.

## Domain model

The decision hierarchy is **macro thesis → asset thesis → strategy → position**.

- A macro thesis is a long-lived belief about a broad economic or market regime.
- An asset thesis is a long-lived belief about one asset or underlying.
- A strategy is a tactical expression of one or more theses.
- A position is the live or historical implementation of a strategy.
- A claim is a provenance-bearing unit of research evidence that can support or challenge a thesis.
- A signal is the auto-derived resolution section of a thesis articulation. Signal linkage lives in
  `signal_entity_links`; the retired `configure-signal` workflow is not a current configuration path.
- A Decision Item is an auditable request for genuine human judgment. Producers surface Decision Items;
  resolvers act on them. Unattended workflows must not fabricate or silently resolve user judgment.

`monitoring` is a position-expression flag, not an information gate. Thesis status is derived during
ingestion by the expression cascade. An expressed thesis is `monitoring`; a previously expressed but flat
thesis becomes `closed` and may return to `monitoring`. Do not reject or delete an intact thesis as cleanup.

## Record and data authority

Remote Supabase is the single source of truth for Trade Journal investment entities. Database reads use
`scripts/psql-query.ts`; writes use purpose-built scripts under `scripts/ops/`, never ad hoc SQL or a generic
provider connector. Everything auditable lands in `journal_entries`. Derived values are computed during
ingestion and stored rather than recomputed on query.

Schema changes begin in `src/db/schema.ts`, include a migration under `migrations/`, are applied immediately
through the configured PostgreSQL client, and are verified. A package or lockfile change also requires the
npm 10 clean-install dry-run gate.

GitHub Issues in `njabrooks/trade-journal` are the Record Authority for repository-owned Work Items. Native
GitHub sub-issues and dependencies are preferred; issue-body text is only a fallback when a Record Authority
lacks the native primitive. Cross-repository coordination links to Trade Journal Work Items and does not
duplicate their authority.

## Federated boundaries

- The Notes/Tana system owns capture and thinking. Promoted investment claims retain provenance and reuse
  existing source claims; Trade Journal owns the resulting investment entities.
- Radon owns IBKR access beyond Flex, including bulk option chains, contract qualification, gateway
  operation, and the Trade Journal client-id range 20–49.
- Workspace owns Workspace Standard and shared Capability contracts. Trade Journal references those
  authorities rather than copying or redefining them.
- Machine-local credentials, browser sessions, Claude/Codex login state, notifications, and IB Gateway
  availability are environmental prerequisites, not repository-owned guarantees.

## Runtime safeguards

The persistent development server is machine managed. Do not start a competing server on its port. A
production build is followed by restarting the existing service because the build can invalidate its route
cache. Scheduled automation retains its explicit off-switches, per-job locking, stale-lock handling, process-
group timeouts, logs, status records, and failure notifications.

Provider-specific tools, prompts, permissions, and packaging remain separately authored. A generated mirror
or generic headless wrapper proves packaging coverage only; it does not prove safe unattended execution or
Adapter Conformance. Live provider-dependent automation must preserve its declared read/write and judgment
boundaries until a separately accepted migration changes them.

## Further context

- `docs/v2/` records product direction and the loose-agent model.
- `docs/adr/` is the canonical location for durable architecture decisions.
- `docs/agents/domain.md`, `docs/agents/issue-tracker.md`, and `docs/agents/triage-labels.md` explain discovery
  and tracker use.
- `docs/agents/provider-adapters/` records supporting interactive and headless adapter inventories.

### docs/agents/domain.md

# Domain Docs

How engineering skills should consume Trade Journal's domain documentation while exploring the repository.

## Layout

Trade Journal is a **single-context repository**. It does not use a root `CONTEXT-MAP.md` or package-scoped domain contexts.

The domain-document locations are:

- `CONTEXT.md` at the repository root;
- `docs/adr/` for repository-wide architecture decision records; and
- the existing `CLAUDE.md`, `AGENTS.md`, and relevant `docs/v2/` material for current operating and product context.

These canonical Workspace Standard discovery surfaces are always present. Do not create speculative terms or
ADRs merely to populate them; the ADR index explicitly records when no standalone ADR has been adopted.

## Before exploring

1. Read `CONTEXT.md`.
2. Read ADRs under `docs/adr/` that touch the area being changed.
3. Read `CLAUDE.md` as the canonical repository operating manual and `AGENTS.md` for Codex-specific differences.
4. Read the relevant `docs/v2/` decisions before designing changes to the product direction, belief layer, or loose-agent model.

## Use the glossary's vocabulary

When output names a domain concept—in an issue title, specification, refactor proposal, hypothesis, or test—use the term defined in `CONTEXT.md`. Do not drift to synonyms that the glossary explicitly avoids.

If a needed concept is not defined, reconsider whether existing vocabulary already covers it. Record a genuine terminology gap for domain modeling rather than silently inventing a competing term.

## Flag ADR conflicts

If proposed work contradicts an existing ADR, surface the conflict explicitly rather than overriding it silently. Identify the ADR and explain the new evidence or direct conflict that would justify reopening the decision.

## Locked Capabilities

### capability:scope:trade-journal/belief-evidence-assessment 1.0.0

Intent: Assess provenance-bearing evidence against governed thesis underwriting without changing investment state or fabricating owner judgment.

Contract: Given an explicit macro or asset thesis identifier, an explicit assessment request, provenance-bearing evidence, and the Registry-resolved thesis-underwriting dependency, bind analysis to the latest versioned thesis articulation and its complete active, thesis-linked, auto-derived resolution-signal set as { thesisId, thesisType, articulationId, articulationVersion, signalIds }; return a thesis-centric assessment for every bound signal with source provenance, direct semantic bearing, findings, quotations, limitations, recommendation, overall summary, and recording disposition; keep analysis read-only unless recording is explicitly delegated; if recording is delegated, persist atomically only through the deterministic repository recorder; and report unavailable, stale, malformed, or failed outcomes without partial writes or invented state.

Provider-neutral instructions:
- Resolve capability:scope:trade-journal/thesis-underwriting through the published Registry Lock and use its governed output as underwriting context; reference that Capability without copying or redefining its contract.
- Require an explicit thesis ID, thesis type, assessment request, and source. Thesis lifecycle status is context only and never an information gate.
- Load the current target through the repository recorder's read-only target mode and bind the assessment to the exact thesis ID, thesis type, articulation ID, articulation version, and complete active signal-ID set; use its returned current articulation, active signals, full linked claims and observations with Toulmin and source content, prior signal evidence, rebuttals, assumptions, timeframe, and notes as the complete assessment surface.
- Carry source provenance as an existing promoted main_claims ID when available; otherwise require source title and type, content SHA-256, and stable URL, file label, observed time, or published time when available. Never create or duplicate a claim.
- Assess each resolution statement in its underwriting context. Ticker and keyword overlap may assist retrieval only; every non-neutral result requires direct semantic bearing on the statement.
- Use only neutral, strengthening, confirmed, weakening, or invalidated in thesis-centric polarity. Every invalidation result must carry the recorder-enforced conditionEffect pair: risk_receding/strengthening, risk_growing/weakening, condition_cleared/confirmed, condition_triggered/invalidated, or no_bearing/neutral.
- Return one assessment for every bound signal, including neutral results, with statement, type, importance, confidence, semantic bearing, evidence summary, findings, short quotations, limitations, and advisory recommendation, plus an overall summary and recording disposition.
- Analysis is read-only by default. When recording is explicitly requested, send the complete envelope only to scripts/ops/record-belief-evidence-assessment.ts --stdin; its serializable transaction revalidates the target immediately before the accepted writes.
- The maximum write set is one qualitative signal_data_snapshots row per targeted signal, one existing-claim claim_signal_evidences link per non-neutral result when a promoted provenance-bearing claim exists, one batch audit record, and one thesis-level signal_evidence_received journal record per non-neutral result.
- Never change thesis or signal status or confidence, create or configure signals, create claims, alter claim-thesis links, create or resolve Decision Items, mutate strategies or positions, place trades, author explicit_details, invoke retired configure-signal behavior, or use an ad-hoc database write.
- Treat an unknown thesis, absent current underwriting, incomplete provenance, malformed result, changed target, unavailable prerequisite, or non-zero recorder result as unavailable or failed with no partial success.

Provider Adapter `belief-evidence-assessment-codex` (current):

## Codex Provider Adapter

1. Require an explicit macro or asset thesis ID, thesis type, assessment request, source, and recording disposition. Resolve `capability:scope:trade-journal/thesis-underwriting` through the published Registry Lock and use that governed Capability as context; do not copy or redefine its contract. Thesis lifecycle status is context only, not an information gate.
2. From the Trade Journal root, use the repository command runner for `npx tsx scripts/ops/record-belief-evidence-assessment.ts --target --thesis-id <uuid> --thesis-type <macro|asset>`. If it returns unavailable, stop. Bind the result to the exact `{ thesisId, thesisType, articulationId, articulationVersion, signalIds }`. The returned context is the complete assessment surface: current articulation, active resolution signals, full linked claims and observations with Toulmin fields and source content, prior signal evidence, rebuttals, assumptions, timeframe, and notes. Do not silently substitute a thinner context.
3. Read the supplied content with Codex's available file or web tools and preserve source provenance. Use an existing promoted `main_claims.id` when supplied; otherwise include source title/type, content SHA-256, and stable URL, file label, observed time, or published time when available. This adapter must not create a claim or duplicate an existing claim.
4. Assess every bound resolution statement. Ticker or keyword overlap may assist retrieval only and cannot determine direction. A non-neutral result requires direct semantic bearing; thematic adjacency, scheduled events without outcomes, missing evidence, or multi-step inference stays neutral with explicit limitations.
5. Use thesis-centric polarity: `strengthening` and `confirmed` mean the thesis strengthened; `weakening` and `invalidated` mean it weakened. For every invalidation signal include `conditionEffect`: receding risk is `risk_receding` + `strengthening`, growing risk is `risk_growing` + `weakening`, a definitively cleared falsification condition is `condition_cleared` + `confirmed`, a triggered invalidation condition is `condition_triggered` + `invalidated`, and no direct bearing is `no_bearing` + `neutral`. The recorder rejects mismatched pairs. Use hard outcomes conservatively.
6. Return one result per bound signal, including neutral results, with statement, type, importance, assessment, confidence, semantic bearing, evidence summary, findings, short quotations, limitations, and advisory recommendation, plus an overall summary and recording disposition. Recommendations are not owner decisions.
7. Unless recording was explicitly requested or delegated, analysis is read-only and the result must say recording was not requested. When recording was explicitly requested, pipe the exact envelope once to `npx tsx scripts/ops/record-belief-evidence-assessment.ts --stdin`. Treat sandbox or network denial, a stale target, unavailable prerequisite, malformed envelope, or non-zero result as failed with no partial success.

The recorder is the sole mutation boundary. This adapter must not use ad-hoc SQL or Drizzle writes, temporary write scripts, generic provider connectors, or generic journal helpers; must not invoke `scripts/ops/update-entity-status.ts`; must not invoke `scripts/ops/resolve-decision.ts`; must not create or configure a signal; and has no thesis-confidence, signal-status, Decision Item, claim-linkage, strategy, position, trade, scheduler, credential, or operational-cutover authority.

### capability:scope:trade-journal/belief-maintenance 1.0.0

Intent: Maintain Trade Journal belief records in bounded passes while reserving genuine investment judgment for explicit Decision Items.

Contract: Given a declared maintenance mode, bounded target set, and optional dry-run choice, inspect current Trade Journal claims, theses, signals, evidence, cursors, and maintenance worklists; perform only the mode's approved mechanical writes; record auditable changes; surface ambiguous or judgment-bearing outcomes as Decision Items; return a machine-readable summary of reads, writes, cursor movement, decisions surfaced, skips, and failures; never silently resolve a genuine decision or change an entity status.

Provider-neutral instructions:
- Accept only maintenance, thesis-review, or claim-backfill modes and require every run to declare its bound before execution.
- Use repository read helpers for database reads and purpose-built operations scripts for writes; never use ad hoc SQL mutation.
- Auto-link or update only deterministic, policy-authorized cases and send ambiguity, contradiction, or weakening to the Decision Item path.
- Preserve producer/resolver separation: surfacing a Decision Item is permitted, resolving one is forbidden.
- Preserve cursor monotonicity and record the before and after cursor whenever a cursor is advanced.
- Write auditable investment changes to journal_entries through the approved repository boundary.
- Return an explicit empty result when no work is due and an explicit failure result when prerequisites or bounded execution fail.

Provider Adapter `belief-maintenance-codex` (current):

## Codex Provider Adapter

Accept a request object with `mode` (`maintenance`, `thesis-review`, or `claim-backfill`), a required `bound` describing the maximum items or explicit identifiers, and `dryRun` (default `true`). Run from the Trade Journal repository root with the configured repository environment.

1. For `maintenance`, load `.agents/skills/maintenance/SKILL.md`; begin with `npx tsx scripts/ops/maintenance-status.ts --json` and drain only the declared bound.
2. For `thesis-review`, load `.agents/skills/thesis-review/SKILL.md` and execute only the requested bounded sub-mode.
3. For `claim-backfill`, load `.agents/skills/backfill-claims/SKILL.md` and process only the declared claim identifiers or item limit.
4. Use the repository command runner, `scripts/psql-query.ts` for reads, and only purpose-built operations named by the selected workflow for writes. Respect sandbox or approval refusal as an unavailable or failed result.
5. Auto-apply only clear mechanical outcomes authorized by the workflow. Surface ambiguity, refutation, weakening, re-underwriting, or any other genuine judgment as a Decision Item.
6. Return one JSON object with `success`, `mode`, `dryRun`, `bound`, `reads`, `writes`, `cursorBefore`, `cursorAfter`, `decisionsSurfaced`, `skipped`, and `errors`. Every write entry must name the repository operation and affected record.

This adapter is a producer. It must not invoke `scripts/ops/resolve-decision.ts`, `scripts/ops/update-entity-status.ts`, or silently re-underwrite a thesis. Missing credentials, Tana unavailability, sandbox denial, an exhausted bound, or partial failure must be reported honestly.

### capability:scope:trade-journal/belief-research-relation 1.0.0

Intent: Relate Notes-owned research to developing or monitoring theses by direct semantic bearing while preserving exact promoted-claim provenance and unresolved judgment boundaries.

Contract: Given one complete current Notes-owned research insight, the complete Trade Journal promoted-claim identity catalog, current developing and monitoring macro and asset thesis arguments, existing governed relationships, and validated Registry-resolved claims-synthesis and research-publication facts where needed, deterministically prepare a provider-neutral recommendation-only bearing result. Reuse exact provenance-bearing claims regardless of lifecycle status; never duplicate or directly publish a Notes source claim. A relation requires direct semantic proof between an exact source claim assertion and a governed thesis argument; holdings, tickers, keywords, and provider recommendations are never proof. Preserve evidence, reasoning, backing, qualifier, every rebuttal, time horizon, relationship rationale, audit context, and explicit ambiguity. Genuine claim-identity or thesis-bearing judgment, every refuting relation, and each tentative supporting or foundational relation becomes an unresolved Decision Item candidate and is never silently resolved. Persistence requires a short-lived exact user authorization and the sole serializable recorder, which may only reuse or insert exact claim_thesis_mappings and insert unresolved Decision Items plus one complete journal audit. Refuse unavailable, stale, malformed, ambiguous, conflicting, expired, or authority-expanding input without partial writes.

Provider-neutral instructions:
- Require one explicit Trade Journal research insight ID. Read it only through scripts/belief-research-relation.ts --prepare --insight-id <uuid>, which loads complete Notes/Tana provenance, the complete promoted-claim identity catalog, all developing and monitoring thesis arguments, and existing governed relationships through approved repository helpers. It exposes no write mode.
- Notes/Tana owns capture, source material, and Toulmin extraction. Trade Journal owns promoted investment claims, theses, governed claim-thesis relationships, publication state, unresolved Decision Items, and journal audit. Preserve source evidence, claim identity, thesis bearing, relationship semantics, authorization, persistence, and audit as distinct concepts.
- Resolve capability:scope:trade-journal/claims-synthesis and capability:scope:trade-journal/research-publication through the published Registry Lock. Claims synthesis and provider output are recommendations, not semantic proof or authorization. A relation may reference only an exact existing provenance-bearing main_claims row or an authority-approved research-publication result that has already created that promoted claim. This recorder never creates main_claims rows.
- Exact (sourceInsightId, sourceClaimId) provenance is deterministic identity regardless of claim lifecycle status. Reuse it and refuse duplicate or ambiguous provenance. Preserve evidence, reasoning, backing, qualifier, every rebuttal, time horizon, tickers, Notes content digest, and explicit ambiguity.
- Judge research by direct semantic bearing on the governed core argument, drivers, or assumptions of developing or monitoring theses. Holdings are not an information gate. Ticker overlap and keyword overlap are retrieval hints only. A provider recommendation is not proof. Every relation must carry exact claim and thesis anchors plus the explicit causal, evidential, qualifying, or refuting connection.
- Validate recommendation-only output through scripts/belief-research-relation.ts --validate-result <file|-> --context <file>. Every source claim must be covered by direct semantic relations, explicit claim_identity or thesis_bearing ambiguity, or an unrelated rationale; never combine ambiguity with a silent link. Keep output bounded to at most 25 source claims and five relations per claim.
- Prepare persistence through scripts/belief-research-relation.ts --prepare-recording --result <file|-> --context <file>. The authorization-ready recording embeds the complete closed validated context and result; validation rederives every relationship, exclusion, and Decision Item candidate before declaring readiness. Direct relations to existing exact provenance claims become relationship candidates. Every refuting relation also becomes an unresolved review_refuting_claim Decision Item; medium- or low-confidence supporting and foundational relations also become unresolved confirm_claim_link Decision Items. Claim-publication requirements are exclusions. Ambiguities with an existing claim anchor become unresolved confirm_claim_link Decision Item candidates; this Capability never resolves them.
- Present exact relationship and Decision Item candidates to the current user. Only that user's explicit selection may be serialized into a version 1.0.0 belief_research_relation_authorization token valid for no more than 24 hours, cryptographically bound to recordingDigest and naming unique acceptedRelationIds and acceptedDecisionIds with the exact contract statement. Never infer, fabricate, broaden, or self-authorize a token.
- Validate the token without writes through scripts/belief-research-relation.ts --validate-authorization <file|-> --recording <file>. When and only when it encodes the current user's explicit decision, pipe the complete { prepared, authorization } envelope once to scripts/ops/record-belief-research-relation.ts --stdin. Provider adapters have no direct database authority.
- The recorder uses one serializable transaction and authorization lock; revalidates the exact current context, provenance identities, developing or monitoring thesis eligibility, and existing mappings; retries serialization and uniqueness races with a fresh snapshot; reuses only an exact relationship or inserts the exact accepted claim_thesis_mappings columns; inserts accepted unresolved decision_required journal entries with resolution null; inserts one belief_research_relation_recorded journal audit with digest-bound authorization, candidates, actual records, exclusions, permitted surface, and forbidden authority; returns exact created/reused dispositions and write counts; and makes canonically identical retries idempotent.
- The maximum tables are claim_thesis_mappings and journal_entries with only the declared columns. Never create or mutate main_claims, research artifacts or insights, thesis state or status, signals, strategies, positions, orders, or trades; never resolve a Decision Item; and never use ad-hoc SQL, Supabase MCP writes, direct API mutation, generic writers, temporary write scripts, or live-provider output as authority.
- Treat missing, incomplete, malformed, stale, unavailable, authority-ambiguous, or conflicting inputs; absent user acceptance; expired authorization; sandbox denial; transaction or audit failure; and provider unavailability as unavailable, refused, or failed with no partial success. Do not invoke a live provider or mutate production data to manufacture evidence.
- Unattended execution may prepare and validate recommendation-only results and return explicit ambiguities. It cannot create an authorization token, invoke the recorder, link a claim, surface a persisted Decision Item, change status, or make genuine judgment. Its genuine unattended eligibility is therefore conditional for read-only preparation and ineligible for all writes.

Provider Adapter `belief-research-relation-codex` (current):

## Codex Provider Adapter

1. Require one explicit Trade Journal research insight ID. From the Trade Journal root, use Codex's repository command runner for `npx tsx scripts/belief-research-relation.ts --prepare --insight-id <uuid>`. Continue only when it returns `status: ready`; otherwise return the exact unavailable result. Bind every recommendation to the exact `contextDigest`, Notes source/content digest, Toulmin evidence, claim resolutions, developing and monitoring thesis argument digests, and existing relationships.
2. Preserve authority exactly. Notes/Tana owns capture, source material, and Toulmin extraction. Trade Journal owns promoted investment claims, theses, governed claim-thesis relationships, publication state, unresolved Decision Items, and journal audit. Do not treat provider output as repository fact, semantic proof, persistence, authorization, or a completed relationship.
3. Resolve the Registry-locked claims-synthesis and research-publication Capabilities. Exact `(sourceInsightId, sourceClaimId)` provenance is identity regardless of claim lifecycle status: reuse the existing promoted claim and never duplicate a Notes-owned source claim. A semantic relation can name only an exact existing promoted claim or a promoted claim already created by an accepted research-publication result. This adapter cannot publish claims.
4. Judge only direct semantic bearing between the exact source claim assertion and a governed developing-or-monitoring thesis core argument, driver, or assumption. A relationship must be `supports`, `refutes`, or `foundation`, preserve honest high, medium, or low confidence and rationale, and include exact claim and thesis anchors plus the causal, evidential, qualifying, or refuting connection. Every refuting relation must also surface an unresolved `review_refuting_claim` Decision Item. Medium- or low-confidence supporting or foundational relations must also surface an unresolved `confirm_claim_link` Decision Item. Holdings are not an information gate. Ticker overlap, keyword overlap, and provider recommendation are never semantic proof.
5. Preserve source evidence, reasoning, backing, qualifier, every rebuttal, time horizon, relationship rationale, audit context, and explicit ambiguity. Cover every source claim with bounded direct relations, an explicit `claim_identity` or `thesis_bearing` ambiguity, or an unrelated rationale. Do not silently link an ambiguous claim and do not invent missing identity or judgment.
6. Validate the recommendation-only result by using Codex's repository command runner for `npx tsx scripts/belief-research-relation.ts --validate-result <file|-> --context <file>`. Then prepare the exact persistence boundary by using Codex's repository command runner for `npx tsx scripts/belief-research-relation.ts --prepare-recording --result <file|-> --context <file>`. The authorization-ready recording embeds the complete closed context and result, and readiness validation rederives every candidate. Claim-publication requirements remain exclusions. Only exact existing-claim relationships and unresolved Decision Item candidates can proceed; preparation deterministically adds the required refuting-evidence and tentative-link Decision Items.
7. Present the exact bounded relationship and Decision Item candidates to the current user. Only after that user explicitly accepts exact relation and decision IDs may this interactive adapter serialize a version `1.0.0` `belief_research_relation_authorization` token. It must set `authorizedBy` to `user`, use a fresh UUID, canonical ISO instants no more than 24 hours apart, bind `recordingDigest`, name unique `acceptedRelationIds` and `acceptedDecisionIds`, and use the exact emitted statement. Never infer, fabricate, broaden, reuse for different canonical content, or self-authorize a token.
8. Validate the token without writes by using Codex's repository command runner for `npx tsx scripts/belief-research-relation.ts --validate-authorization <file|-> --recording <file>`. Validation is readiness only. On missing user acceptance, malformed or expired authority, stale bytes, unavailable input, sandbox denial, or user refusal, stop with no writes.
9. When and only when the exact validated token encodes the current user's explicit decision, pipe `{ "prepared": <exact prepared recording>, "authorization": <exact token> }` once to `npx tsx scripts/ops/record-belief-research-relation.ts --stdin`. Treat a non-zero result as failed and do not attempt another mutation route. Retry an uncertain response only with canonically identical authorization JSON.
10. The recorder is the sole mutation boundary. Its serializable transaction may reuse or insert only exact `claim_thesis_mappings`, insert accepted unresolved `decision_required` `journal_entries` with `resolution: null`, and insert one `belief_research_relation_recorded` audit containing digest-bound authorization, candidates, actual records, exclusions, permitted write surface, and forbidden authority. It revalidates current provenance and thesis eligibility, returns exact dispositions, retries safe races, is idempotent, and rolls back partial failure.
11. This adapter must not create or mutate `main_claims`, artifacts, insights, theses or thesis status, signals, strategies, positions, orders, or trades; must not resolve a Decision Item; must not use ad-hoc SQL or Drizzle writes, Supabase MCP writes, direct API mutation, generic writers, or temporary write scripts; and has no scheduler, credential, provider-discovery, active-operational, or trade authority.

Unattended execution may prepare or validate recommendation-only output and return explicit ambiguity. It must not create an authorization token, invoke the recorder, persist a link or Decision Item, resolve judgment, or change status. Return `writes: []`.

### capability:scope:trade-journal/claims-synthesis 1.0.0

Intent: Distinguish and relate Notes-owned research evidence to Trade Journal investment claims and theses without duplicating provenance-bearing claims or silently exercising investment judgment.

Contract: Given an explicit Notes-owned Trade Journal research-insight handoff, load a deterministic SHA-256-bound context in which Notes/Tana owns capture, source material, and Toulmin extraction while Trade Journal owns promoted investment entities and their governed relationships; keep source evidence, existing main claims, synthesized investment claims, and proposed thesis mappings exactly distinct; deterministically reuse any existing main claim with exact (sourceInsightId, sourceClaimId) provenance; preserve counterarguments, qualifiers, and source provenance; consider direct semantic bearing across developing and monitoring asset or macro theses without treating ticker or keyword overlap as proof; return a bounded recommendation-only result with explicit ambiguity and no writes; validate the result against the exact context digest; and report unavailable source, database, environment, or provider states without invented context or mutation.

Provider-neutral instructions:
- Require one explicit research insight ID. Prepare context only through scripts/claims-synthesis.ts --prepare --insight-id <uuid>; this deterministic repository boundary performs approved reads and exposes no write mode.
- Accept a source only when its Trade Journal research artifact proves Notes/Tana provenance. Notes/Tana owns capture, source material, and Toulmin extraction; Trade Journal does not recreate those source records as new authority.
- Treat source evidence, existing main claims, synthesized investment claims, and proposed thesis mappings as separate collections. Never call a source claim an investment claim until the result explicitly recommends a distinct synthesis or reuses an exact existing main claim.
- For every source claim, exact (sourceInsightId, sourceClaimId) provenance is deterministic identity: reuse the existing main_claims ID even if its lifecycle is not active, and refuse any duplicate synthesized claim.
- A synthesized investment claim is only a recommendation for a materially distinct investment assertion. It must retain the sourceClaimId provenance reference and preserve every source counterargument and qualifier; it is not permission to create a database row.
- Judge thesis bearing against both developing and monitoring macro or asset theses. Ticker and keyword overlap are retrieval hints only and never semantic proof. Every ambiguity must name exactly one axis: claim_identity for uncertain identity or distinction, or thesis_mapping for uncertain relationship, direction, or thesis bearing. A claim_identity ambiguity must have no reused or synthesized claim resolution. A thesis_mapping ambiguity may preserve a deterministic claim resolution but must have no mapping for that source claim.
- Return at most 25 recommendations, at most 25 synthesized claims, at most 25 ambiguities, and at most 5 proposed mappings per source claim. Use only supports, refutes, or foundation relationships and high, medium, or low confidence.
- Set execution exactly to { mode: recommendation_only, writes: [] }. Provider adapters must not promote, create, link, or mutate claims; change thesis or signal status; resolve Decision Items; mutate strategies or positions; place or stage trades; or use ad-hoc SQL, Supabase MCP writes, direct API mutation, or any repository operation script.
- Validate provider output only through scripts/claims-synthesis.ts --validate-result <file|-> --context <file>. A validation refusal, unavailable prerequisite, stale context digest, malformed output, provider failure, or sandbox denial is a complete unavailable or failed outcome, never partial success.
- Unattended execution is eligible only for the same recommendation-only contract: it must preserve ambiguity, emit no writes, and stop on unavailable inputs. Any accepted persistence remains a separate explicitly authorized governed capability.

Provider Adapter `claims-synthesis-codex` (current):

## Codex Provider Adapter

1. Require one explicit Trade Journal research insight ID. From the repository root, use the repository command runner for `npx tsx scripts/claims-synthesis.ts --prepare --insight-id <uuid>`. Continue only when it returns `status: ready`; otherwise return the exact unavailable result. Bind all work to the returned `contextDigest` computed from the full context.
2. Preserve the authority split exactly: Notes/Tana owns capture, source material, and Toulmin extraction; Trade Journal owns promoted investment entities and governed relationships. The source artifact must prove Notes/Tana provenance. Do not recreate Notes capture or treat the provider as source authority.
3. Keep all four concepts distinct in the result: `sourceEvidence` identifies Notes-owned evidence; `existingMainClaims` identifies reused Trade Journal claims; `synthesizedInvestmentClaims` contains only materially distinct proposed investment assertions; and `thesisMappings` contains only proposed governed relationships. Do not collapse these collections or describe a recommendation as a completed write.
4. Exact `(sourceInsightId, sourceClaimId)` provenance is deterministic identity. Return `reuse_exact_provenance` with the exact existing `main_claims.id`, regardless of that claim's lifecycle status. Never synthesize a duplicate of that Notes-owned source claim. For a genuinely distinct synthesis, retain `sourceClaimId`, the exact source qualifier, every source rebuttal, and a concrete synthesis rationale.
5. Assess direct semantic bearing against the full eligible macro and asset thesis catalog, including both developing and monitoring theses. Ticker and keyword overlap are retrieval hints only; they do not prove claim identity, relationship, direction, or thesis bearing. Every ambiguity must name exactly one `axis`: use `claim_identity` for uncertain identity or distinction, with no reused or synthesized resolution; use `thesis_mapping` for uncertain relationship, direction, or thesis bearing, which may retain a deterministic claim resolution but must return no thesis mapping. In either case return `defer_ambiguous`.
6. Return the exact version `1.0.0` result shape: every source claim exactly once in `sourceEvidence`; deterministic exact-provenance reuse; at most 25 synthesized claims; at most 25 ambiguities; at most 25 recommendations; and at most 5 proposed thesis mappings per source claim. Use only `supports`, `refutes`, or `foundation`, and only `high`, `medium`, or `low` confidence. Set `execution` exactly to `{ "mode": "recommendation_only", "writes": [] }` and state material limitations.
7. Save the proposed JSON only to a temporary or explicitly requested local file, then use the repository command runner for `npx tsx scripts/claims-synthesis.ts --validate-result <file|-> --context <prepared-context-file>`. A stale digest, unknown source/claim/thesis reference, missing provenance reuse, lost qualifier or rebuttal, ambiguous mapping, malformed or oversized result, unavailable input, provider failure, or validation refusal is a complete failed or unavailable outcome. Do not partially accept it or infer missing state.
8. This adapter is recommendation-only. It must not invoke `scripts/ops/create-claim.ts`, `scripts/ops/batch-create-claims.ts`, `scripts/ops/link-claim-to-thesis.ts`, `scripts/relate-research.ts` apply modes, `scripts/ops/update-entity-status.ts`, or `scripts/ops/resolve-decision.ts`; must not use ad-hoc SQL, Supabase MCP writes, or direct API mutation; must not promote, create, link, or mutate claims; and has no thesis/status, signal, Decision Item, strategy, position, or trade authority. Unattended use is eligible only under this same zero-write boundary and must preserve genuine ambiguity rather than guessing.

### capability:scope:trade-journal/morning-attention-brief 1.0.0

Intent: Synthesize fresh upstream Trade Journal state into one date-scoped attention brief without mutating the belief layer.

Contract: Given a brief date and a deterministic morning bundle with explicit upstream freshness, synthesize one headline, at most five ranked attention items, and a concise body; if required upstream state is stale or missing, identify that limitation in the result; persist at most one morning_briefs row for the date through an idempotent upsert; and never mutate belief entities, journal history, Decision Items, recommendations, positions, or statuses.

Provider-neutral instructions:
- Require a brief date and gather only the deterministic repository morning bundle.
- Inspect and report freshness for thesis observation, maintenance, options advice, portfolio, decisions, and calendar inputs before synthesis.
- Do not re-query, browse, or invent evidence beyond the supplied bundle.
- Rank no more than five genuine attention items and preserve actionable deep links without resolving the underlying judgment.
- Persist only through the date-keyed morning brief upsert and report whether the row was inserted or replaced.
- On a same-date rerun, preserve idempotent exactly-one-row behavior.
- Never write to belief-layer entities, journal_entries, Decision Items, advisor recommendations, positions, strategies, or status fields.

Provider Adapter `morning-attention-brief-codex` (current):

## Codex Provider Adapter

Accept `briefDate` and optional `dryRun`; reject a missing or invalid date. From the Trade Journal root, load `.agents/skills/morning-brief/SKILL.md` and gather the sole input with `npx tsx scripts/morning-brief-data.ts --json`.

Check the bundle's timestamps and stale flags for the required upstream producers. Use only that bundle: do not browse, re-query for extra evidence, or fill missing state with assumptions. Produce one headline, zero to five ranked attention items, the bounded Markdown body, and metadata containing the bundle generation time and input counts.

Before synthesis, pass `briefDate` and the bundle's `producerFreshness` object unchanged to `npx tsx capabilities/morning-attention-brief/evaluate-inputs.ts`. Do not derive or replace an absent producer declaration. If the evaluator returns `success:false`, return that result unchanged: do not browse, re-query, synthesize, or invoke a write operation.

When not in dry-run, persist only with `npx tsx scripts/ops/save-morning-brief.ts --stdin`; the operation is keyed by `brief_date`, so a same-date rerun replaces the existing row and leaves exactly one row. Return JSON containing `success`, `briefDate`, `freshness`, `headline`, `attention`, `persisted`, `write`, `unavailableInputs`, and `errors`.

This adapter is synthesis-only. It must not invoke any other write operation, write `journal_entries`, raise or resolve a Decision Item, save advisor recommendations, or mutate a thesis, claim, signal, strategy, position, or status. Missing credentials or sandbox denial is an explicit unavailable or failed result.

### capability:scope:trade-journal/portfolio-options-advice 1.0.0

Intent: Produce bounded, portfolio-aware options recommendations without acquiring trade-execution authority.

Contract: Given either morning-batch or LEAP mode, current portfolio and regime context, standing constraints, and eligible market-data access, generate and judge bounded option candidates, live-verify every selected structure where required, persist only genuine recommendation batches with the established dashboard result shape, return explicit empty or unavailable outcomes, and never place, route, stage, or modify an order.

Provider-neutral instructions:
- Require an explicit mode: morning-batch for the six non-LEAP scenarios or LEAP for long-dated call candidates.
- Read current portfolio, regime, thesis, volatility, chain, and constraint context through repository-owned read boundaries.
- Treat the Radon-managed IBKR gateway and quote path as an environmental prerequisite; do not copy or redefine Radon infrastructure.
- Live-verify selected structures and reject stale, unavailable, or materially changed quotes rather than fabricating a recommendation.
- Persist only genuine, bounded recommendations using the approved recommendation-batch operation; an empty candidate set or unavailable verification writes nothing.
- Preserve the existing recommendation payload used by dashboard consumers and report mode, candidates, verification, persistence, skips, and failures.
- Never place, route, stage, preview, or modify a trade or order.

Provider Adapter `portfolio-options-advice-codex` (current):

## Codex Provider Adapter

Accept `mode` (`morning-batch` or `leap`), optional bounded scenario/ticker filters, and `maxRecommendations` (maximum five per scenario). From the Trade Journal root, load `.agents/skills/options-advisor/SKILL.md` and read the latest regime before candidate judgment.

For `morning-batch`, run only hedge, income, collar, put_entry, risk_reversal, and opportunistic scenarios. For `leap`, require eligible US market hours and check the Radon-managed IBKR gateway on port 4001 before running only `leap_entry`. Use `npx tsx scripts/options-advisor.ts --scenario <scenario>` for candidate math. Live-verify selected contracts through the documented Radon-backed quote boundary; unavailable connectors, sandbox denial, or materially stale verification removes the candidate.

Persist a scenario only when genuine recommendations remain, using only `npx tsx scripts/ops/save-advisor-recommendations.ts --stdin`. Preserve engine-produced structure, metrics, and volatility context. Return JSON containing `success`, `mode`, `regime`, `candidates`, `recommendations`, `verification`, `writes`, `skipped`, `unavailableInputs`, and `errors`.

This adapter has recommendation authority only. It must never call an order, trade, execution, preview, or staging operation. Empty candidate sets, unavailable verification, or gateway failure write no recommendation batch.

### capability:scope:trade-journal/portfolio-snapshot 1.0.0

Intent: Read and present the current Trade Journal portfolio state without mutation.

Contract: Given an optional list of account UUIDs and a human or JSON output choice, return the latest available portfolio snapshot from Trade Journal's remote source of truth, including snapshot date, NAV, cash, leverage, owner and underlying breakdowns, strategies, unlinked positions, and cash balances; report unavailable data or execution failures honestly and perform no writes.

Provider-neutral instructions:
- Use the repository's portfolio snapshot command as the sole data-access boundary.
- Use the latest available per-account snapshots and preserve their reported snapshot date rather than implying wall-clock freshness.
- Pass optional account filtering only as explicit account UUIDs.
- Return the command's human-readable result when a human summary is requested and its JSON result for programmatic consumption.
- Treat portfolio state as read-only and perform no database, journal, order, strategy, thesis, or position mutation.
- Report missing credentials, database unavailability, malformed filters, and command failures explicitly rather than substituting cached or invented portfolio state.

Provider Adapter `portfolio-snapshot-codex` (current):

## Codex Provider Adapter

1. Translate a JSON request to `--format json`; otherwise request the human-readable result.
2. Pass an account filter only as `--account-ids` followed by a comma-separated list of account UUIDs.
3. Use the repository command runner from the Trade Journal root to invoke `npx tsx scripts/pull-portfolio.ts` with only those supported flags.
4. Return the command result with its reported snapshot date. For headless or programmatic work, preserve the complete JSON object as the result payload.
5. Treat missing credentials, sandbox or network denial, database errors, invalid UUID filters, and non-zero execution as explicit unavailability or failure.

This adapter is read-only. Its authority ends at presenting the returned snapshot; it does not mutate Trade Journal state or place trades.

### capability:scope:trade-journal/research-pipeline 1.3.0

Intent: Provide provider-neutral, zero-write results across research-pipeline intake, research, evidence synthesis, thesis expression, user gates, and graduation planning while composing the ordered aggregate and contracting superseded legacy procedures to rollback-capable protective tombstones.

Contract: Given one explicit Trade Journal research insight, return independently invocable, deterministic, bounded, zero-write results for pipeline_status, idea_intake, thesis_formalization, unknown_mapping, research_preparation, unknown_research, evidence_synthesis, thesis_expression, gate_decision, or graduation, or compose exact stage results with Registry-locked claims-synthesis, research-publication, belief-research-relation, and thesis-underwriting dependencies into the ordered aggregate lifecycle. Notes/Tana owns capture, source material, and Toulmin extraction. Trade Journal stage results preserve exact provenance, source-linked evidence, reasoning, backing, qualifier, every rebuttal, time horizon, contradictions, ambiguity, audit context, idempotency, and genuine user judgment without persisting anything. Research preparation is portable and provider-neutral; unavailable research remains unavailable; evidence synthesis retains unresolved contradictions; expression provides no position size or trade instruction; gate recommendations remain judgment_required until the user records an audited decision; graduation is a provenance-bound handoff plan that remains judgment_required until the user accepts its exact digest, preserves decline as refused, proposes initial lifecycle developing, and has no persistence or status authority. The aggregate remains coordination-only and zero-write. The ten former Claude procedures and ten headless projections are protective tombstones that preserve their discovery paths and exact governed replacements but cannot execute legacy file or database persistence; rollback remains available from accepted pre-contraction revision 051c1c57c9dd447c930e4352262d6c4cd6f90fe2. No stage, aggregate, or tombstone has scheduler, credential, production-data, status, Decision Item resolution, signal configuration, strategy, position, order, trade, generic write, or database authority.

Provider-neutral instructions:
- Require one explicit research insight UUID. Build the ten stage results through scripts/research-pipeline.ts --pipeline-status, --idea-intake, --thesis-formalization, --unknown-mapping, --research-preparation, --unknown-research, --evidence-synthesis, --thesis-expression, --gate-decision, or --graduation with <file|-> input; validate stage bytes with --validate-stage-result. Describe, evaluate, and validate the aggregate through --describe, --evaluate, and --validate-result.
- Use the exact ordered lifecycle: pipeline_status, idea_intake, thesis_formalization, unknown_mapping, research_preparation, unknown_research, evidence_synthesis, claims_synthesis, research_publication, belief_research_relation, thesis_expression, gate_decision, graduation.
- Validate and compose all ten package-owned stages through their stage-result validators. Resolve claims-synthesis, research-publication, belief-research-relation, and thesis-underwriting only through the published Registry Lock. Preserve exact digests; do not copy stage or dependency validators into the aggregate.
- Preserve exact (sourceInsightId, sourceClaimId) provenance, evidence, reasoning, backing, qualifier, every rebuttal, time horizon, ambiguity, relationship rationale, and digest-bound user judgment audit (decision ID, actor ID, recorded-at time) through the stage-owned contracts. Reuse provenance-bearing claims. Holdings, tickers, keywords, or provider recommendations are never semantic proof.
- Every stage result uses execution mode stage_result_only with writes: []. The aggregate uses aggregate_coordination_only with writes: []. Neither can create files, publish claims, persist relationships, change status, create or resolve Decision Items, mutate theses or signals, configure signals, mutate strategies, positions, orders, or trades, or use ad-hoc SQL, Supabase MCP writes, direct API mutation, generic writers, schedulers, credentials, provider discovery, or production-data evidence.
- When research-publication returns its exact judgment boundary, report judgment_required and name only scripts/ops/publish-research.ts --stdin. When belief-research-relation returns its exact recording judgment boundary, report judgment_required and name only scripts/ops/record-belief-research-relation.ts --stdin. Never invoke either operation or create an authorization token from aggregate authority.
- Represent missing stage material as incomplete; unavailable prerequisites as unavailable; digest or freshness mismatch as stale; policy, malformed authority, or user refusal as refused; provider or execution failure as failed; genuine unresolved user judgment as judgment_required; and a validated zero-write stage result as ready. Research uses at least two independent source domains and explicit HTTPS citations. Evidence synthesis requires exact unknown coverage, retains contradictions, and derives advance, hold, kill, or modify from decision-critical resolution and posterior confidence. Expression requires an audited user entry decision, recommends no position size, and leaves act, watch, or discard to the final user gate. Graduation reuses exact provenance, depends on locked governed capabilities, and prepares only a developing thesis handoff; monitoring remains expression-cascade derived. Do not collapse partial progress into success.
- Keep all ten former Claude discovery paths and ten headless projections as protective tombstones. Each tombstone names its exact governed CLI flag, refuses the removed legacy procedure, performs no writes, and preserves historical rollback to accepted pre-contraction revision 051c1c57c9dd447c930e4352262d6c4cd6f90fe2. Do not perform the final provider-discovery cutover owned by issue #76.
- Retry only canonically identical aggregate input. The aggregate digest is the retry key. A changed input, source digest, stage result, authorization boundary, package binding, or adapter binding is a new evaluation, not a retry.
- Unattended use is conditional only for read-only pipeline status, portable preparation, validation of explicitly supplied evidence, and aggregate describe, evaluate, and validation modes through the governed adapter. Provider research may run only with declared source access and must report unavailable environments honestly. Idea selection, thesis classification, every user gate, graduation acceptance, and every accepted write remain interactive and ineligible. Tombstones are never execution authority.

Provider Adapter `research-pipeline-codex` (current):

## Codex Provider Adapter

1. Require one explicit Trade Journal research insight UUID. From the Trade Journal root, use Codex's repository command runner with `npx tsx scripts/research-pipeline.ts`. Build independently invocable zero-write results through `--pipeline-status`, `--idea-intake`, `--thesis-formalization`, `--unknown-mapping`, `--research-preparation`, `--unknown-research`, `--evidence-synthesis`, `--thesis-expression`, `--gate-decision`, or `--graduation` with `<file|->`; validate stage bytes through `--validate-stage-result`. Use `--describe`, `--evaluate`, and `--validate-result` for the aggregate.
2. Preserve authority exactly. Notes/Tana owns capture, source material, and Toulmin extraction. Trade Journal owns promoted claims, publication state, governed relationships, theses, unresolved Decision Items, and journal audit. All ten stage results and the aggregate coordinate only and write nothing.
3. Preserve exact source insight, source claim, content digest, evidence, reasoning, backing, qualifier, every rebuttal, time horizon, source URLs, contradictions, ambiguity, digest-bound user judgment audit, and prior-stage digest. Reuse provenance-bearing claims. Holdings, tickers, keywords, and provider recommendations are never semantic proof of claim identity or thesis bearing.
4. Preserve the four accepted 1.1.0 intake/formalization seams unchanged: supplied-only pipeline status, audited user idea selection and classification, falsifiable formalization with its exact failure-mode rules and user gate, and user-advanced unknown mapping with decisive-unknown, resolvability, priced-in, kill-condition, and research-payoff rules.
5. `research_preparation` produces a portable provider-neutral brief from the exact accepted thesis, unknown, and research track. Do not require Claude Desktop, a connector, or a provider-specific citation surface. `unknown_research` requires explicit HTTPS citations and at least two independent source domains; if browsing, credentials, or source access is unavailable, report `unavailable` rather than fabricating evidence. Permit at most one result for each exact `(unknownId, track)` pair while allowing distinct falsification, validation, and analogue tracks for the same unknown.
6. `evidence_synthesis` binds every supplied track-result digest, covers every mapped unknown at least once, retains unresolved contradictions, and derives advance, hold, kill, or modify from posterior confidence, kill conditions, core-mechanism support, and decision-critical gaps. It does not update a file, confidence record, database entity, status, signal, or Decision Item.
7. `thesis_expression` requires an audited user decision to enter Stage 5 and returns value-chain analysis, candidates, expression-failure risk, entry/exit/review criteria, and sizing inputs without a position size, quantity, allocation, order, or trade instruction. `gate_decision` remains `judgment_required` until the user records act, watch, or discard and may preserve an explicit user override.
8. `graduation` is a provenance-bound handoff plan only. Bind claim identity and any existing main-claim reuse to the exact validated Registry-locked claims-synthesis context and result; route later persistence through Registry-locked research-publication and belief-research-relation; use Registry-locked thesis-underwriting for articulation with rebuttal-derived resolution. Return `judgment_required` until the user accepts the exact handoff and claims binding by its boundary digest; preserve an explicit decline as `refused`. A proposed thesis starts at `developing`; `monitoring` remains expression-cascade derived. Do not invoke a recorder, create or update an entity, configure a signal, or mutate status.
9. Every stage returns `execution: { mode: "stage_result_only", writes: [] }`. Compose stage results through their public validators and exact digest chain. Resolve governed dependencies through the published Registry Lock without copying or loosening their contracts. The aggregate remains `execution: { mode: "aggregate_coordination_only", writes: [] }`.
10. Report each aggregate stage as `incomplete`, `unavailable`, `stale`, `refused`, `failed`, `judgment_required`, or `ready`. Retry only canonically identical input using its digest. Missing or unavailable external prerequisites are never a partial success.
11. The ten former Claude procedures and ten headless projections are protective tombstones. Each retained discovery path names its exact governed stage flag and must not execute the removed legacy procedure. Historical rollback is available from accepted pre-contraction revision `051c1c57c9dd447c930e4352262d6c4cd6f90fe2`; tombstones themselves have no persistence authority. Do not perform the final issue #76 provider-discovery cutover.
12. If the exact research-publication stage returns `judgment_required`, the only named persistence operation is `scripts/ops/publish-research.ts --stdin`. If belief-research-relation returns its recording judgment boundary, the only named operation is `scripts/ops/record-belief-research-relation.ts --stdin`. Never invoke either from aggregate or stage authority.
13. This adapter must not use ad-hoc SQL, Supabase MCP writes, direct API mutation, generic writes, schedulers, credentials, provider discovery, or production-data evidence; must not change status, resolve a Decision Item, configure signals, mutate a strategy or position, or place or stage an order or trade.
14. Unattended execution is conditional only for read-only status, portable preparation, validation of explicitly supplied evidence, and aggregate describe, evaluate, and validation through the governed adapter. Provider research may run only with declared source access and honest unavailable behavior. Idea selection, classification, every user gate, graduation acceptance, and every accepted write remain interactive and ineligible. Protective tombstones are never execution authority.

### capability:scope:trade-journal/research-publication 1.0.0

Intent: Publish explicitly accepted research claims and governed thesis relationships without duplicating Notes-owned provenance or expanding publication into investment-state authority.

Contract: Given a validated recommendation-only result from the Registry-resolved claims-synthesis Capability, re-read the complete current Notes-owned source and Trade Journal claim/thesis context; preserve the exact distinction between source evidence, synthesis recommendations, existing promoted claims, an explicit user publication decision, and governed claim-thesis relationships; deterministically prepare only unambiguous thesis-related candidates; require a short-lived user authorization token cryptographically bound to the exact prepared publication digest and naming every accepted claim and relationship; and, only through the purpose-built repository recorder, atomically reuse exact provenance claims, create accepted draft investment claims, create accepted claim-thesis relationships, and record one complete journal audit. Refuse unavailable, stale, malformed, ambiguous, conflicting, expired, or authority-expanding input without partial writes.

Provider-neutral instructions:
- Resolve capability:scope:trade-journal/claims-synthesis through the published Registry Lock and accept only its validated version 1.x recommendation-only result. Claims synthesis recommends; it never authorizes publication.
- Require one explicit Trade Journal research insight ID and the exact claims-synthesis result. Prepare publication only through scripts/research-publication.ts --prepare --insight-id <uuid> --synthesis-result <file|->. The deterministic boundary re-reads approved repository state and exposes no write mode.
- Preserve five distinct concepts: Notes-owned sourceEvidence, synthesizedRecommendations, existing or proposed Trade Journal claimCandidates, an explicit user authorization decision, and governed relationshipCandidates. Never collapse recommendation into authorization or relationship into claim identity.
- Exact (sourceInsightId, sourceClaimId) provenance is identity. Reuse an existing provenance-bearing main_claims row; never duplicate it. A create candidate must preserve the source claim, evidence, reasoning, backing, qualifier, every rebuttal, time horizon, tickers, and synthesis rationale.
- Exclude every claim-identity or thesis-bearing ambiguity from authorization candidates. Ticker and keyword overlap are retrieval hints only, not semantic proof. Publication requires an unambiguous claim resolution and at least one unambiguous developing-or-monitoring thesis relationship.
- Present the bounded prepared result to the user. An interactive adapter may serialize only the user's explicit exact selection into a research_publication_authorization version 1.0.0 token. The token must be user-authorized, valid for no more than 24 hours, bind publicationDigest, name unique acceptedClaimRefs and acceptedRelationshipIds, include every and only claim used by an accepted relationship, and use the exact contract statement. Never infer, fabricate, broaden, or self-authorize a token.
- Validate a supplied token without writes through scripts/research-publication.ts --validate-authorization <file|-> --publication <file>. Validation success is readiness, not publication.
- When and only when the exact token records the current user's explicit publication decision, send the complete { prepared, authorization } envelope once to scripts/ops/publish-research.ts --stdin. This is the sole mutation boundary and provider adapters have no direct database authority.
- The recorder uses one serializable transaction and an authorization lock; deterministically re-derives a unique prepared candidate set; revalidates current source, claim, and thesis context; retries serialization or uniqueness races with a fresh snapshot; reuses exact or concurrently-created matching provenance claims only for accepted candidates; inserts only accepted new main_claims rows at draft status; reuses claim_thesis_mappings only when relationship, confidence, and rationale notes exactly match or inserts the accepted mapping; inserts one research_publication_recorded journal_entries audit containing a compact digest-bound snapshot of the validated authorization, accepted candidates, and actual persisted records; and returns exact created/reused dispositions and write counts. Canonically identical JSON retries return the recorded result; changed canonical authorization content, duplicate logical relationships, or conflicting provenance/relationship state is refused.
- The maximum tables are main_claims, claim_thesis_mappings, and journal_entries with the exact columns declared by the prepared result. Never write research_artifacts, research_insights, main_claim_evidence, theses, signals, Decision Items, strategies, positions, orders, or trades; never change entity or thesis status; never resolve a Decision Item; and never use ad-hoc SQL, Supabase MCP writes, direct API mutation, generic write helpers, temporary write scripts, legacy Obsidian paths, removed intelligence-report storage, triage systems, or views terminology.
- Treat unavailable source/database/environment/provider state, incomplete provenance, malformed or stale digests, ambiguous candidates, missing or expired authorization, user non-acceptance, transaction failure, or audit failure as unavailable, refused, or failed with no partial success. Do not invoke a live provider or mutate production data to manufacture evidence.
- Unattended execution is ineligible to authorize or publish because claim selection and thesis-bearing acceptance are genuine user judgment. Headless use may only prepare or validate and return authorization_required or unavailable with writes: [].

Provider Adapter `research-publication-codex` (current):

## Codex Provider Adapter

1. Require one explicit Trade Journal research insight ID and one already validated recommendation-only result from the Registry-resolved `capability:scope:trade-journal/claims-synthesis`. Claims synthesis is input recommendation, never publication authorization.
2. From the Trade Journal root, use Codex's repository command runner for `npx tsx scripts/research-publication.ts --prepare --insight-id <uuid> --synthesis-result <file|->`. Continue only when it returns `status: authorization_required`; otherwise return the exact unavailable result. Bind all work to its `publicationDigest`, current Notes source digest, claims-synthesis result digest, candidates, exclusions, and exact permitted write surface.
3. Preserve authority and meaning exactly. Notes/Tana owns capture, source material, and Toulmin extraction. Trade Journal owns promoted investment entities, publication state, and governed relationships. Keep `sourceEvidence`, `synthesizedRecommendations`, `claimCandidates`, the user's publication decision, and `relationshipCandidates` distinct. Never describe a recommendation, readiness result, or token proposal as a completed publication.
4. Exact `(sourceInsightId, sourceClaimId)` provenance is deterministic identity. Reuse an existing provenance-bearing claim regardless of lifecycle status and never duplicate it. A new draft claim candidate must preserve evidence, reasoning, backing, qualifier, every rebuttal, time horizon, tickers, and synthesis rationale. Do not use ticker or keyword overlap as semantic proof.
5. Do not offer excluded, claim-identity-ambiguous, or thesis-bearing-ambiguous material for publication. Every accepted claim must have at least one unambiguous accepted developing-or-monitoring macro or asset thesis relationship. Use only the exact prepared candidate IDs; do not invent claims, mappings, or missing judgment.
6. Present the exact bounded candidate selection to the current user. Only after that user explicitly accepts exact claim refs and relationship IDs may this interactive adapter serialize that decision into the version `1.0.0` `research_publication_authorization` token. It must set `authorizedBy` to `user`, use a fresh UUID, canonical ISO instants no more than 24 hours apart, bind the exact `publicationDigest`, name unique `acceptedClaimRefs` and `acceptedRelationshipIds`, contain every and only claim used by an accepted relationship, and use the exact statement emitted by the prepared contract. Never infer, fabricate, broaden, reuse for different canonical content, or self-authorize a token.
7. Validate the token without writes by using Codex's repository command runner for `npx tsx scripts/research-publication.ts --validate-authorization <file|-> --publication <file>`. Validation means authorization readiness only. On missing user acceptance, malformed or expired authority, stale bytes, unavailable input, sandbox denial, or user refusal, stop with no writes.
8. When and only when the exact validated token encodes the current user's explicit decision, pipe `{ "prepared": <exact prepared result>, "authorization": <exact token> }` once to `npx tsx scripts/ops/publish-research.ts --stdin`. Treat a non-zero result as failed; do not attempt a different mutation route. Retry after an uncertain response is safe only with canonically identical authorization JSON.
9. The recorder is the sole mutation boundary. Its serializable transaction may reuse or insert accepted `main_claims` rows, reuse or insert accepted `claim_thesis_mappings`, and insert one `research_publication_recorded` `journal_entries` audit containing a compact, digest-bound snapshot of the validated authorization, accepted claims, and accepted relationships. It deterministically re-derives candidates, revalidates current state, retries concurrency races with a fresh snapshot, refuses incompatible conflicts or drift, returns exact created/reused dispositions, and rolls back partial failure.
10. This adapter must not use ad-hoc SQL or Drizzle writes, Supabase MCP writes, direct API mutation, generic write helpers, temporary write scripts, legacy Obsidian paths, removed intelligence-report or triage systems, or stale views terminology; must not write artifacts, insights, evidence links, theses, signals, Decision Items, strategies, positions, orders, or trades; must not change any entity or thesis status or resolve any Decision Item; and has no scheduler, credential, provider-discovery, live-operational, or trade authority.

Unattended execution may prepare or validate only and must return `authorization_required` or an exact unavailable/refused result with `writes: []`. It cannot create an authorization token or invoke the recorder because candidate acceptance and thesis-bearing acceptance are genuine user judgment.

### capability:scope:trade-journal/thesis-observation 1.0.0

Intent: Sense current evidence against high-materiality thesis signals without making or resolving investment decisions.

Contract: Given a governed observation time and a bounded set of due Tier-1 monitoring theses, gather current attributable sources and fresh price context, assess each active qualitative signal from the thesis's perspective, persist only observation artifacts, signal evidence snapshots, and their audit history, and return a directive per-signal result; never create or resolve Decision Items or mutate thesis, strategy, claim, or signal status.

Provider-neutral instructions:
- Select observations from the repository's deterministic due-observation bundle and preserve its Tier-1 bound.
- Use current attributable sources and report source or price unavailability rather than filling gaps with assumptions.
- Assess the signal statement in thesis-centric polarity and preserve neutral results when evidence is not discriminating.
- Write only the approved observation report, signal_data_snapshots evidence, and corresponding journal history.
- Do not raise or resolve Decision Items and do not change any entity status.
- Return complete per-signal directives, source references, changed-from-prior state, write records, and failures.

Provider Adapter `thesis-observation-codex` (current):

## Codex Provider Adapter

Accept `asOf`, an optional explicit `thesisIds` subset, and `maxTheses`; reject an unbounded request. From the Trade Journal root, load `.agents/skills/thesis-observe/SKILL.md` and gather the due bundle with `npx tsx scripts/ops/find-theses-due-observe.ts --json`.

Use the available current web research capability for attributable, recent sources and the repository's supplied price context. Treat missing connectors, sandbox or network denial, stale prices, and unavailable sources as explicit unavailable inputs. For collector-tracked signals, preserve the deterministic deferral in the bundle.

Produce the workflow's directive report with signal identifier, score, evidence, assessment, and change from prior, then use only the documented thesis-observe ingestion boundary. Return JSON containing `success`, `asOf`, `thesesObserved`, `signalsAssessed`, `directives`, `writes`, `unavailableInputs`, and `errors`.

This adapter is sensing-only. Its writes are limited to the observation artifact, `signal_data_snapshots`, and corresponding journal history. It must not invoke `scripts/ops/resolve-decision.ts`, `scripts/ops/update-entity-status.ts`, raise a Decision Item, or change thesis, strategy, claim, or signal status.

### capability:scope:trade-journal/thesis-underwriting 1.0.0

Intent: Build a living thesis underwriting from provenance-bearing evidence without changing the Trade Journal thesis lifecycle or reviving retired manual signal configuration.

Contract: Given an identified macro or asset thesis, its complete linked provenance-bearing claims and observations, prior articulation, relevant compositional context, and any available signal-quality inputs, produce a new versioned thesis articulation and derived resolution signals; preserve claim linkage and rebuttal handling, require relation of in-scope unlinked claims before writing, refine the resolution section from actionable quality or candidate inputs, carry forward prior signal lineage whenever a statement continues it, record only the bounded articulation, signal, candidate-resolution, and audit writes supplied by the repository scripts, does not change thesis status, and reports unavailable inputs or execution failures explicitly.

Provider-neutral instructions:
- Read thesis context only through the repository snapshot and query helpers, including linked claims, observations, prior articulations, compositional context, signal-quality diagnostics, and candidate signals where available.
- Before synthesis, require all in-scope evidence to be linked: if thesis-snapshot reports unlinked claims, report the relation prerequisite and do not write a new articulation until the evidence set is complete.
- Preserve provenance by grounding the articulation and each derived signal in linked claim or observation identifiers; treat sparse provenance-bearing observations as valid evidence with appropriate weight.
- Derive qualitative confirmation, invalidation, and completion signals from linked claims, counterarguments, assumptions, timeframes, and legitimate thesis dependencies.
- When signal-quality diagnostics or candidate signals are non-empty, refine the resolution section by sharpening or dropping chronic-neutral statements, covering material gaps, and promoting or dismissing every assessed candidate.
- Use a new articulation version for every completed underwriting and set supersedesSignalId whenever a new resolution statement continues a prior signal, preserving eligible statement-to-sensor lineage.
- Never author manual signal thresholds, data sources, explicit_details, or retired configure-signal behaviour.
- Do not change macro or asset thesis status; the expression-driven cascade owns thesis lifecycle state.
- Do not resolve Decision Items or represent synthesized confidence as an unrequested owner decision; interactive refinement remains available, and headless use requires an explicit delegated underwriting request for the identified thesis.
- Use only the repository articulation and candidate-resolution scripts for bounded writes, retain their journal audit trail, and report unavailable database, malformed thesis, missing evidence, or command failures without substituting invented state.

Provider Adapter `thesis-underwriting-codex` (current):

## Codex Provider Adapter

1. Require an explicit macro or asset thesis identifier and a delegated underwriting request. For headless execution, require both the thesis identifier and type; do not infer an owner decision or authority from a scheduled or ambient request.
2. Use the repository command runner from the Trade Journal root to load complete thesis context with `npx tsx scripts/ops/thesis-snapshot.ts --id <thesis-id> --type <asset|macro>`. Check `thin.unlinkedClaimCount`; if it is non-zero, report the relation prerequisite and do not write a new articulation until those claims are related. Then inspect linked claims, observations, prior articulation, compositional context, signal-quality diagnostics, and candidate signals before synthesizing.
3. Preserve linked-claim and observation provenance in the new articulation. Derive qualitative confirmation, invalidation, and completion signals from the case's drivers, assumptions, rebuttals, timeframe, and legitimate dependencies. When signal-quality diagnostics or candidate signals are non-empty, must sharpen or drop chronic-neutral statements, cover material gaps with grounded signals, and promote or dismiss every assessed candidate signal. Set `supersedesSignalId` whenever a new resolution statement continues a prior signal so statement-to-sensor lineage is preserved.
4. Never create manual signal thresholds, data sources, or `explicit_details`; do not invoke retired configure-signal behaviour. Interactive work may present a draft for refinement, while headless work returns the delegated synthesis and its assumptions explicitly.
5. Preview the bounded write with `npx tsx scripts/insert-thesis-articulation.ts --input <articulation.json> --dry-run`, then use the same command without `--dry-run` only for the requested new versioned articulation and derived resolution signals. Record each promoted candidate's resulting signal identifier, and resolve or dismiss every assessed candidate signal through `scripts/ops/resolve-candidate-signal.ts`.
6. Report sandbox or network denial, missing database access, an unknown thesis, incomplete evidence, malformed input, or non-zero command results as unavailable or failed. Preserve the returned articulation identifier, signal count, and audit outcome.

This adapter may write a new versioned articulation, derived resolution signals, consumed candidate-signal dispositions, and their resulting audit history. It must not change thesis status, must not invoke `scripts/ops/update-entity-status.ts`, must not invoke `scripts/ops/resolve-decision.ts`, must not write ad hoc SQL, and has no order, trade, execution, or scheduler authority.
